#!/bin/bash

exec > >(tee -i install.log) 2>&1
set -euo pipefail

# 🎨 Couleurs
RED="\e[31m"
GREEN="\e[32m"
YELLOW="\e[33m"
BLUE="\e[34m"
MAGENTA="\e[35m"
CYAN="\e[36m"
BOLD="\e[1m"
RESET="\e[0m"

function erreur {
    echo -e "${RED}❌ Erreur : $1${RESET}"
    echo -e "${YELLOW}⚠️  Veuillez contacter l'éditeur${RESET}"
    echo -e "📧 Email : ${CYAN}james.gbetchedji@epitech.eu${RESET}"
    echo -e "📞 Tel : ${CYAN}+22956549199${RESET}"
    exit 1
}

# Vérif root
if [ "$(id -u)" -ne 0 ]; then
    echo -e "${RED}❌ Ce script doit être exécuté en tant que root.${RESET}" >&2
    exit 1
fi

clear
sleep 1

echo -e "${CYAN}${BOLD}"
echo "🚀========================================🚀"
echo "     SCRIPT D'INSTALLATION DU DUMPS"
echo "             BY ${GREEN}NOGE${CYAN}"
echo "🚀========================================🚀"
echo -e "${RESET}"
sleep 2

echo -e "${YELLOW}⚡ Veuillez suivre ce script attentivement${RESET}"
sleep 2
echo -e "${YELLOW}❓ Des questions peuvent être posées${RESET}"
sleep 2

clear
sleep 2

echo -e "${MAGENTA}📦 Ajout du dépôt Universe...${RESET}"
add-apt-repository -y -s universe || erreur "Échec de l'ajout du dépôt universe"

echo -e "${MAGENTA}🔄 Mise à jour des paquets...${RESET}"
apt update || erreur "Échec de la mise à jour des paquets"

echo -e "${MAGENTA}⬆️  Mise à niveau du système...${RESET}"
apt upgrade -y || erreur "Échec de la mise à niveau des paquets"

echo "wireshark-common wireshark-common/install-setuid boolean true" | debconf-set-selections

# 📦 Installation packages
echo -e "${GREEN}📥 Installation des paquets nécessaires...${RESET}"
packages_list=(
    avr-libc build-essential ca-certificates-java cabal-install cargo
    clang clang-tools cmake curl diffutils elfutils elpa-tuareg emacs-nox
    ffmpeg flac gcovr ghc golang haskell-stack ksh liballegro5-dev
    libboost-all-dev libc-bin libc-dev-bin libc-devtools libc6
    libc6-dbg libcsfml-dev libcsfml-doc libcunit1 libcunit1-dev libelf-dev
    libfreetype-dev libgmp-dev libgmp10 libgmp10-doc libgmp3-dev
    libgmpxx4ldbl libgudev-1.0-dev libirrlicht-dev libirrlicht1.8
    libjpeg-turbo8-dev libncurses-dev libopenal-dev libsdl2-dev libsfml-doc
    libuuid1 libvirt-dev libvorbis-dev libx11-doc libx11-xcb-dev
    libxcb-image0 libxcb-image0-dev libxcb-util-dev libxcb-util0-dev
    libxcb-util1 libxcursor-dev libxext-dev libxext-doc libxi-dev
    libxinerama-dev libxrandr-dev locales ltrace nasm ncurses-base net-tools
    npm nodejs openjdk-21-jdk openjdk-21-jre php-bcmath php-dev php-gd
    php-mbstring php-mysql php-phar-io-version php-gettext php-tokenizer
    python3 python3-pycryptodome python3-dev python3-numpy python3-pip
    python3-pyte python3-requests python3-yaml qt6-base-dev qt6-base-dev-tools
    rlwrap ruby rustc strace sudo systemd-dev tar tcpdump tcsh tmux tree unzip
    uuid-dev valgrind vim virt-manager wireshark x264 zip zsh libcriterion-dev
    libsfml-dev libsfml-doc libc6 libstdc++6 libqt5core5t64 libqt5gui5t64 libqt5network5t64
    libqt5widgets5t64 libqt5printsupport5t64 libqt5webchannel5 libqt5webenginewidgets5 libqt5xml5t64
)
export DEBIAN_FRONTEND=noninteractive
apt -y install "${packages_list[@]}" || erreur "Échec de l'installation des paquets"

echo -e "${BLUE}🐳 Installation de Docker...${RESET}"
apt remove -y docker docker.io containerd runc || true
apt update
apt -y install ca-certificates curl gnupg apt-transport-https software-properties-common 
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
apt update
apt install docker-ce -y 
systemctl enable --now docker || erreur "Echec de l'installation de Docker "
sudo systemctl status docker

echo -e "${GREEN}⚙️ Installation outils Epitech...${RESET}"
add-apt-repository -y -s ppa:epitech/ppa
apt update
export DEBIAN_FRONTEND=noninteractive
apt install -y epitech-cpool epitech-emacs epitech-vim
apt purge -y postfix

snap install teams-for-linux

echo -e "${BLUE}🛠️ Installation de clang-20 et epiclang...${RESET}"
apt install -y wget
wget -O - https://apt.llvm.org/llvm-snapshot.gpg.key | tee /etc/apt/trusted.gpg.d/llvm.asc
echo "deb [arch=amd64 signed-by=/etc/apt/trusted.gpg.d/llvm.asc] https://apt.llvm.org/noble/ llvm-toolchain-noble-20 main" | tee /etc/apt/sources.list.d/llvm.list
wget -O - https://epitech.github.io/epiclang/key.asc | tee /etc/apt/trusted.gpg.d/epiclang-apt.asc
echo "deb [arch=amd64 signed-by=/etc/apt/trusted.gpg.d/epiclang-apt.asc] https://epitech.github.io/epiclang/ testing main" | tee /etc/apt/sources.list.d/epiclang.list
apt update
apt install -y clang-20 epiclang
update-alternatives --install /usr/bin/clang clang /usr/bin/clang-20 100
update-alternatives --install /usr/bin/clang++ clang++ /usr/bin/clang++-20 100

echo -e "${CYAN}📜 Installation du coding-style...${RESET}"
wget -O - "https://raw.githubusercontent.com/nogebeat/deb-ubuntu-epitech/refs/heads/main/header-epitech.sh" | bash -s || erreur "Echec headers Epitech"
wget https://raw.githubusercontent.com/Epitech/coding-style-checker/main/coding-style.sh -P /tmp/  || erreur "Echec du coding-style Noge"
chmod +x /tmp/coding-style.sh
mv /tmp/coding-style.sh /usr/local/bin/coding-style

usermod -aG docker $USER || erreur "Echec ajout groupe docker"


docker --version

cd /tmp
git clone https://github.com/nogebeat/coding-style-checker.git

cd coding-style-checker
chmod +x new-style.sh
cp new-style.sh /usr/local/bin/new-style
sudo cp lambdananas /usr/local/bin
sudo chmod +x /usr/local/bin/lambdananas

echo -e "${BLUE} Installation de Algobox .... ${RESET}"

# Téléchargement
wget -q https://www.xm1math.net/algobox/assets/files/algobox_1.1.1_ubuntu_24_04_amd64.deb -O /tmp/algobox.deb \
    || erreur "Erreur lors du téléchargement de Algobox "

# Installation brute avec dpkg
sudo dpkg -i /tmp/algobox.deb

# Correction des dépendances si besoin
if [ $? -ne 0 ]; then
    echo -e "${YELLOW} 📦 Résolution des dépendances manquantes... ${RESET}"
    sudo apt-get install -f -y || erreur "Impossible de résoudre les dépendances de Algobox"
fi

echo -e "${GREEN} ✅ Algobox a été installé avec succès ! ${RESET}"



clear
echo -e "${GREEN}${BOLD}"
echo "✅================================================✅"
echo "   🎉 Installation terminée avec succès ! 🎉"
echo "   👨‍💻 Coding-Style by ${CYAN}NOGE${GREEN} prêt à l'emploi"
echo "✅================================================✅"
echo -e "${RESET}"

echo -e "💡 Commandes disponibles :"
echo -e "   - ${YELLOW}coding-style . .${RESET} → Vérifier avec le style officiel"
echo -e "   - ${YELLOW}new-style . .${RESET} → Vérifier avec la version ${MAGENTA}optimisée de NOGE${RESET}"
echo
echo -e "${RED}⚠️  Pensez à redémarrer votre PC après l'installation !${RESET}"
